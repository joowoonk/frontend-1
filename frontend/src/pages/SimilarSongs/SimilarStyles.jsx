import styled from "styled-components";
import { button, songInfo } from "../styles/ComponentStyleConstants";

const SimilarStyles = styled.div`
  ${songInfo};

  button {
    ${button};
  }
`;

export default SimilarStyles;

import React from "react";

const Play = (props) => {
  return (
    <div>
      <h2>{props.track.track_name}</h2>
      {/* <p> {props.monster.email}</p> */}
    </div>
  );
};

export default Play;
